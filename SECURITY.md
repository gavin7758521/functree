# 安全说明

如果你发现 FuncTree 的安全问题，请不要公开披露细节。

当前首版安全边界：

- 默认无账号系统。
- 推荐部署在本机、内网或受信任环境。
- 不应暴露到公网作为生产服务。
- 数据库文件默认位于 `data/functree.db`。

安全要求：

- 不提交数据库文件。
- 不提交 `.env`。
- 不在日志里输出密钥、令牌或敏感凭据。
- 所有 API 输入必须校验。
- 所有 SQL 必须参数化。
